import { Box, Typography } from '@mui/material';

export const Contacts_Toolbar = () => (
  <Box>
    <Typography align='center' sx={{ m: 1 }} variant="h4">Контакты</Typography>
    <Box sx={{ mt: 3 }}>
    </Box>
  </Box>
);
